# name: Backend Release

# on:
#   workflow_dispatch:
#   push:
#     branches:
#       - main
#     paths:
#       - "apps/api/**"
#       - "infra/helm/**"
#       - "infra/scripts/deploy-prod.ps1"
#       - ".github/workflows/backend-release.yml"

# permissions:
#   contents: read
#   id-token: write

# env:
#   AWS_REGION: ${{ vars.AWS_REGION || 'eu-central-1' }}
#   EKS_CLUSTER_NAME: ${{ vars.EKS_CLUSTER_NAME }}
#   HELM_VALUES_FILE: ${{ vars.HELM_VALUES_FILE || 'infra/helm/values/prod.yaml' }}
#   API_IMAGE_REPOSITORY: ${{ vars.API_IMAGE_REPOSITORY }}
#   API_SERVICE_ACCOUNT_ROLE_ARN: ${{ vars.API_SERVICE_ACCOUNT_ROLE_ARN }}
#   WORKER_SERVICE_ACCOUNT_ROLE_ARN: ${{ vars.WORKER_SERVICE_ACCOUNT_ROLE_ARN }}
#   BACKEND_RELEASE_NAME: ${{ vars.BACKEND_RELEASE_NAME || 'pytorch-model' }}
#   BACKEND_NAMESPACE: ${{ vars.BACKEND_NAMESPACE || 'pytorch-model-prod' }}

# jobs:
#   release:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: ${{ secrets.AWS_GITHUB_ACTIONS_ROLE_ARN }}
#           aws-region: ${{ env.AWS_REGION }}

#       - uses: aws-actions/amazon-ecr-login@v2

#       - uses: azure/setup-helm@v4
#         with:
#           version: v3.16.3

#       - uses: azure/setup-kubectl@v4
#         with:
#           version: v1.31.0

#       - name: Compute image tag
#         id: meta
#         run: echo "image_tag=sha-${GITHUB_SHA}" >> "$GITHUB_OUTPUT"

#       - name: Build API image
#         run: docker build -t "${API_IMAGE_REPOSITORY}:${{ steps.meta.outputs.image_tag }}" apps/api

#       - name: Push API image
#         run: docker push "${API_IMAGE_REPOSITORY}:${{ steps.meta.outputs.image_tag }}"

#       - name: Update kubeconfig
#         run: aws eks update-kubeconfig --region "${AWS_REGION}" --name "${EKS_CLUSTER_NAME}"

#       - name: Deploy backend release
#         shell: pwsh
#         run: >
#           ./infra/scripts/deploy-prod.ps1
#           -ReleaseName "${env:BACKEND_RELEASE_NAME}"
#           -Namespace "${env:BACKEND_NAMESPACE}"
#           -ValuesFile "${env:HELM_VALUES_FILE}"
#           -ApiImageRepository "${env:API_IMAGE_REPOSITORY}"
#           -ApiImageTag "${{ steps.meta.outputs.image_tag }}"
#           -ApiServiceAccountRoleArn "${env:API_SERVICE_ACCOUNT_ROLE_ARN}"
#           -WorkerServiceAccountRoleArn "${env:WORKER_SERVICE_ACCOUNT_ROLE_ARN}"
