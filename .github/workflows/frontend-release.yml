# name: Frontend Release

# on:
#   workflow_dispatch:
#   push:
#     branches:
#       - main
#     paths:
#       - "apps/astro-web/**"
#       - "packages/ui/**"
#       - "packages/tailwind-config/**"
#       - ".github/workflows/frontend-release.yml"

# permissions:
#   contents: read
#   id-token: write

# env:
#   AWS_REGION: ${{ vars.AWS_REGION || 'eu-central-1' }}
#   FRONTEND_BUCKET_NAME: ${{ vars.FRONTEND_BUCKET_NAME }}
#   FRONTEND_DISTRIBUTION_ID: ${{ vars.FRONTEND_DISTRIBUTION_ID }}
#   PUBLIC_API_BASE_URL: ${{ vars.PUBLIC_API_BASE_URL }}

# jobs:
#   release:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - uses: oven-sh/setup-bun@v2
#         with:
#           bun-version: 1.3.11

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22.12.0

#       - name: Install dependencies
#         run: bun install --frozen-lockfile

#       - name: Build frontend
#         working-directory: apps/astro-web
#         env:
#           PUBLIC_API_BASE_URL: ${{ env.PUBLIC_API_BASE_URL }}
#         run: bun run build

#       - uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: ${{ secrets.AWS_GITHUB_ACTIONS_ROLE_ARN }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Sync Astro build to S3
#         run: aws s3 sync apps/astro-web/dist "s3://${FRONTEND_BUCKET_NAME}" --delete

#       - name: Invalidate CloudFront cache
#         run: aws cloudfront create-invalidation --distribution-id "${FRONTEND_DISTRIBUTION_ID}" --paths "/*"
