# name: Infra Validate

# on:
#   pull_request:
#     paths:
#       - "infra/**"
#       - ".github/workflows/infra-validate.yml"
#   push:
#     branches:
#       - main
#     paths:
#       - "infra/**"
#       - ".github/workflows/infra-validate.yml"
#   workflow_dispatch:

# jobs:
#   terraform:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: 1.9.8

#       - name: Terraform fmt
#         run: terraform fmt -check -recursive infra/terraform

#       - name: Terraform init
#         run: terraform -chdir=infra/terraform/environments/prod init -backend=false

#       - name: Terraform validate
#         run: terraform -chdir=infra/terraform/environments/prod validate

#   helm:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - uses: azure/setup-helm@v4
#         with:
#           version: v3.16.3

#       - name: Lint backend chart
#         run: helm lint infra/helm/charts/backend-stack -f infra/helm/values/prod.example.yaml

#       - name: Render backend chart
#         run: helm template pytorch-model infra/helm/charts/backend-stack -f infra/helm/values/prod.example.yaml > /tmp/backend-manifests.yaml

#       - name: Lint platform add-ons chart
#         run: helm lint infra/helm/charts/platform-addons -f infra/helm/values/addons.example.yaml

#       - name: Render platform add-ons chart
#         run: helm template platform-addons infra/helm/charts/platform-addons -f infra/helm/values/addons.example.yaml > /tmp/platform-addons-manifests.yaml
