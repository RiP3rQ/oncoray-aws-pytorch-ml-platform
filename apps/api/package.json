{
    "name": "fastapi-core-api",
    "private": true,
    "scripts": {
      "build": "uv sync",
      "check-types": "uv run python -m compileall main.py src tests",
      "dev": "fastapi dev",
      "test": "uv run pytest app/tests",
      "migrate": "uv run alembic upgrade head"
    }
  }
  