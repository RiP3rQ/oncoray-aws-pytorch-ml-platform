[project]
name = "optic-models"
version = "0.1.0"
description = "ViT and EfficientNet-B2 optic models."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"