{
  "name": "pytorch-engine",
  "private": true,
  "scripts": {
    "build": "uv sync",
    "check-types": "uv run python -m compileall src",
    "train-vit": "uv run python -m vit",
    "train-effnet": "uv run python -m effnet"
  }
}
