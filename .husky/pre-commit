cd "$(dirname "$0")/../" || exit 1

if [ ! -f "./node_modules/lint-staged/bin/lint-staged.js" ]; then
  echo "husky - pre-commit: missing node dependencies. Run 'bun install'." >&2
  exit 1
fi

node ./node_modules/lint-staged/bin/lint-staged.js --verbose --concurrent false
